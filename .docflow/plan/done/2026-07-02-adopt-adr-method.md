# Adopt the documentation-led ADR method

Owning ADR: `adr/0001-record-architecture-decisions.md`

Scope: bootstrap the docflow convention set — `AGENTS.md`, `CLAUDE.md`,
`CONVENTIONS.md`, ADR templates (capability + technology), seed ADR
0001, `INDEX.md`, `GLOSSARY.md`, the `plan/` queue, and the `_agent/`
coordination files (single-agent mode).

Exit criteria: scaffolding committed; seed ADR status `Implemented`;
INDEX regenerated.

---
Shipped: docflow bootstrap commit (immediately after tag `v1.0.0`,
baseline `1a2638c`). The bootstrap itself is this item's completion
event.
